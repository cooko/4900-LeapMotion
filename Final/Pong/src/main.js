// The document ready function is called when the page data has been fully received.
// We don't want to be doing anything until we're sure that the DOM is ready.
// Note that this is a jQuery function call. More info: http://api.jquery.com/ready/
$(document).ready(function() {
    GameManager.init();
});
